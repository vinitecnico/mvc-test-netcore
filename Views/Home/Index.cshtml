@model List<MvcTest.Models.Book>
@{
    ViewData["Title"] = "Home Page";
}

<a asp-action="create" asp-controller="Home">Create</a>

</a>
<div class="text-center">
 
 <ul>
    @foreach (var item in Model)
    {
        <li>@item.Name</li>
    }
 </ul>
</div>